import React, {useState,useEffect} from 'react';
import Axios from "axios"

/*
function Test(){
    const [User,setUser] = useState("")

    useEffect(()=>{
        Axios.post("/graduateGuide").then((res)=>{
            if(res.data){
                console.log(res.data);
                setUser(res.data);
            }else{
                alert("failed to");
            }

        });


    },[]);
    return(
        <div className="test">
            {User.map((User,index)=>(
                <p key={index}>
                    {User.lname}
                </p>
            ))}
        </div>
    );
}

export default Test;*/
